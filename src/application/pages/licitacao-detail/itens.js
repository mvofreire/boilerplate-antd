import React from "react";
import { Typography, Row, Col } from "antd";
const { Title } = Typography;

export default ({}) => {
  return (
    <Row type="flex">
      <Col xs={24}>
        <Title level={3}>Itens</Title>
      </Col>
    </Row>
  );
};
